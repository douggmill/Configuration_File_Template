Python module for configuration files.

This Module allows the use of variables to be stored into .txt or .ini files.

It comes in handy if the code is compiled into a .exe installer and variables need to be changed later.

This way those variables may be changed without access to the source code.

